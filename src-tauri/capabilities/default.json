{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Default permissions for the quota auditor window.",
  "windows": ["main"],
  "permissions": ["core:default"]
}
